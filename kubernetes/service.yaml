apiVersion: v1
kind: Service
metadata:
  name: app-demo
  labels:
    app: app-demo
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: app-demo